#include<stdio.h>
#include<stdlib.h>

void initQueue(&Q);
void destoryQueue(&Q);
void clearQueue(&Q);
void queueEmpty(Q);
void GetHead(Q,&e);获得队首元素，并用e返回其值，但是并不出队
void EnterQueue(&Q,e);将元素e入队
void DeleteQueue(&Q,&e);将元素出队，并用e返回其值

void main()
{
	
}
